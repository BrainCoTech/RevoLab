# Intentionally left without ignore rules on the test branch.
# This branch tracks local project artifacts such as checkpoints, logs,
# outputs, caches, and generated package metadata for backup/sharing.
